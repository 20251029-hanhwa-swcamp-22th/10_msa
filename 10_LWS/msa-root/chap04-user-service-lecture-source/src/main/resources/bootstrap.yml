# 서버 부팅시 제일 먼저 작동되는 파일
# implementation 'org.springframework.cloud:spring-cloud-config-server'
#config-sever가 관리하는  msa-config.yml파일을 읽어옴
spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: msa-config